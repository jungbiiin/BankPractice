spring:
  application:
    name: bank
  datasource:
    url: jdbc:mysql://localhost:3306/db_bank?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
